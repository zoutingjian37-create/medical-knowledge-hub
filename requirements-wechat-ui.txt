# Optional Windows-only visual discovery layer. OCR runs locally and observes
# only rendered pixels; the core public-link parser remains independent.
rapidocr_onnxruntime==1.2.3
pyautogui==0.9.54
pyperclip>=1.9,<2
psutil>=7,<8
Pillow>=12,<13
pywinauto>=0.6.9,<0.7

# WeChat 4.1 hides its semantic UI tree on many installations. pywinauto is
# used only for reliable handle-based restore/focus; OCR owns content reading.
